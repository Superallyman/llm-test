import './App.css';
import React, { useState } from "react";

function App() {
  const apiKey = process.env.REACT_APP_API_KEY;
  console.log(apiKey);
  console.log(process.env);

  return (
    <div className="App">
    </div>
  );
}

export default App;
